# Studio qualification record

Observed on Windows/Chrome, 6 September 2026, against the local working tree. This is a bounded development record, not a released-SHA or cross-browser certificate.

## Observed human production path

- Paired a disposable loopback instance with a one-time operator code. No user `.env`, provider purchase or personal media was used.
- Admitted a synthetic source; edited its scene title and body; saved an immutable project revision.
- Prepared a silent landscape plan, reviewed its limits, checked the human review box, approved and then separately started the render.
- Observed the actual job complete with MP4, WebM, SRT, VTT, poster, provenance and QA receipts.
- Retrieved the MP4 through an authenticated request, checked its size and SHA-256, and played the resulting browser Blob to its four-second end without a media error. Dimensions: 1280 × 720.
- MP4 SHA-256: `f48eaeb023444c49c5749c7fc0b611a13057755ba4d2d9feba1664dc8421e50e`.
- Edited the scene and observed the previous plan and approval leave the interface. Unit/API tests separately enforce revision binding.
- Reloaded and paired using a newly issued code. Listed and reopened the saved server project without restoring credentials or approvals from browser storage.
- Checked a narrow 487 CSS-pixel viewport: content width and scroll width were both 468 pixels. Also inspected the light theme. This is not a full responsive/accessibility certification.
- Browser console collection during the initial production path returned no errors or warnings.

## Agent and WebMCP evidence

- Shared transport tests include a real official MCP client/server handshake, tool listing and tool calls against the controlled local service.
- Chrome accepted registration of the eight native `document.modelContext` tools after explicit enablement for a saved project.
- The in-page native inspection attempt failed. Stage-specific diagnostics on a fresh build established successful discovery of the same-origin tool, followed by a browser rejection of its read-only invocation (`NATIVE_TOOL_EXECUTION_FAILED`). The methods are present; absence is not the cause. End-to-end invocation remains unverified; no polyfill or alternate permission was added.
- No external registry grade or indexation is claimed by this record.
- No business tool can create human authorization, publish, pay, perform account login, or accept arbitrary filesystem paths.

## Procedural 3D and claim-provenance evidence

- The same pinned Three.js module powers the real WebGL preview and the fresh, offline Chromium production renderer. No account, persistent browser profile or remote model is involved.
- A one-second, 1280×720, 30 fps `diagram` scene was saved and rendered through the visible Chrome workflow. MP4 and WebM passed file/codec/duration/brightness QA, and the MP4 played to its end without a media error.
- The first 3D MP4 hash was `f9daa1ce74878f29acfc4e985a21c1ce8181b1089a07dc9f5f71817d5c1b9b89`. Visual inspection found that some template builders discarded editable labels; this was fixed and a test now preserves all eight accepted labels in each of the six graph templates.
- The corrected diagram export hash is `51ebfd9da2d63eacc759fd13de2be0df10181469e95e62d1fc09fcdce70e42c4`; its MP4 and WebM QA pass. Its poster was visually inspected: labels and the distinct suspended-statement banner are present. Typography and overlay spacing still need refinement.
- The statement remains `suspended` in the receipted claim sidecar. It is never silently changed to a verified fact. The source body remains separate from the warning banner.
- Python tests use matching Playwright Chromium 1187, a real 320×320 capture, explicit `t0 → t0.5 → t0` pixel equality, cancellation before process launch, final-frame byte-limit cleanup and the real FFmpeg export. Node graph tests alone are not pixel qualification.
- Editing labels or statements prevents save/approval and scene switching until Apply or Cancel; a regression test covers the otherwise-stranded editing lock. Native external WebMCP invocation remains a separate, open qualification.

## Reproducible automated checks

Run from the repository root in the tested Python 3.13 virtual environment and pinned Node/npm environment:

```powershell
python -m pytest -q
python -m pip check
npm --prefix apps/studio run check
npm --prefix apps/studio audit --audit-level=low
```

The frontend suite exercises strict contracts, draft recovery, undo, admission intent, save/plan/approval/run separation, invalidation after edits, metadata-only project lists, byte verification, native tool registration lifecycle, stale context, concurrency and safe retries. Backend tests cover additional authorization/refusal paths. Simulations are not runtime provider qualification.

## Still open

- A fresh CI run on a committed SHA. Integrated scoped-token Chrome retest completed a second four-second MP4/WebM render; MP4 hash `e2a6a3591b0814e48c1b699fd69fe3d224a7aed12b79af0e1dd8c9ea07c6710a`, decoded at1280×720 without a media error.
- Full keyboard journey, focus recovery, screen-reader checks, 200/400% zoom and measured contrast.
- Actual installation and journeys for the new companion in each browser claimed by the release.
- Native browser-agent execution and external registry report/grade.
- Pixel/layout qualification of all six 3D templates, external 3D imports, audio/transcription, avatars, authorized production connectors, vision inference and Scholarium integration.
- Full retention/deletion lifecycle. The browser currently rejects artifact previews larger than 100 MB rather than allocating unbounded memory.
- Caption files are exported; synchronized caption display in the browser player is not yet qualified.

Do not reinterpret a render receipt as confirmation of factual accuracy, source licensing, teaching outcomes, provider availability or public publication.
